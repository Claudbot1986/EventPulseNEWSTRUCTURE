/**
 * Persian/Farsi dictionary — empty placeholder (Språkstöd 2026-09-21).
 *
 * One tag covers both Iranian Persian (`fa-IR`) and Afghan Dari
 * (`fa-AF` / `prs`); user decision 2026-09-21. Strings fall back to
 * en → sv → key-echo until a translator fills in keys. LTR UI retained;
 * Persian text within strings will render RTL per Unicode bidi rules.
 */
export default {};
