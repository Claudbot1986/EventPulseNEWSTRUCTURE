{
  "name": "eventpulse-analytics",
  "version": "0.1.0",
  "description": "User activity analytics backend for EventPulse — port 7778. GDPR-compliant, opt-in, anonymous device IDs. Local-only service.",
  "private": true,
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "start": "tsx server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
