mission_id: EP-2026-08-24-smoke-fi-959
original_prompt: Fix the event details screen spacing.
task_type: expo-ui
subsystems:
  - expo_ui
complexity: small
risk: low
execution_mode: single_agent
roles:
  - lead
  - expo_engineer
verification_profile: expo
context:
  tier0: [.claude/eventpulse/policy.md]
  tier1: [docs/MASTERPLAN.md, docs/BACKLOG.md, .claude/rules/common/hooks.md, .claude/rules/common/agents.md]
  tier2: [06-UI/app.json, 06-UI/App.js]
  tier3: [06-UI/services/eventServiceClient.js]
acceptance_criteria:
  - '"expo_typecheck passes via `cd 06-UI && npx tsc --noEmit`."'
  - '"expo_lint passes via `cd 06-UI && npx eslint . --max-warnings=0` (if configured; else log ''optional, skip'')."'
  - '"expo_smoke passes via `cd 06-UI && npm run verify-providers`."'
  - '"No simulated extraction in UI code that surfaces event counts."'
constraints:
  - '"Do not edit 06-UI/services/eventServiceClient.js (Tier 0 anon read path)."'
  - '"No simulated extraction in UI code that surfaces event counts."'
  - '"No Zustand/Redux/React Query without explicit approval (codebase is intentionally thin)."'
  - '"No full `expo build` after every UI change — reserve for high-impact changes only."'
  - 'PRESERVE WORKING TREE: Working tree has 603 uncommitted change(s); preserve unrelated edits..'
unknown_assumptions:
  - User intent inferred from prompt keywords; please correct if wrong.
escalation_conditions:
  - 'If the task touches a file outside this mission''s scope → stop and escalate.'
  - If a required gate fails → fix or escalate to ep-lead.
required_gates:
  - expo_typecheck
  - expo_lint
  - expo_smoke
requires_user_approval: []
classification_confidence: 0.75
human_review_required: false
planning_only: false
user_overrides: []
working_tree_fp: sha256-djb2:6cc73b52:dirty576
repo_state:
  branch: main
  head_sha: fa7c7a8
  dirty: true
  changed_files_count: 603
  preservation_warning: Working tree has 603 uncommitted change(s); preserve unrelated edits.
  captured_at: 2026-08-24T20:23:27.192Z
compiler_version: epc-1.1
created_at: 2026-08-24T20:23:26.959Z
