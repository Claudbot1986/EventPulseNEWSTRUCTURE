# Dependencies
node_modules/

# OS files
.DS_Store
*.log

# Local env
.env
.env.local
.env.mobile   # MOBILE_CONTROL_TOKEN + PORT + BIND (auto-generated by start-mobile-control.sh, mode 600)

# Build artifacts
dist/
build/

# IDE
.vscode/
.idea/

# Temporary
*.tmp
*.bak
*.swp

# Temp directory
TEMP/

# Local source data (keep out of Git)
/sources/
/rawSources/
/RawSources/
/rawsources/
/01-Sources/RawSources/
/01-Sources/ALL_SOURCES.md

# Runtime queues, generated reports, extracted events, and local agent data
/runtime/
/Alltools-E2E/runtime/
/09-MobileControl/runtime/
/AUTONOMOUS-QUARANTINE/09-MobileControl/runtime/
**/reports/
/02-Ingestion/C-htmlGate/reports/
/03-Queue/03-extractedevents/
/conversations/

# Obsidian vault (persistent project memory, not implementation)
# Per CLAUDE.md Vault Memory Protocol — vault follows project, not git.
/00-Vault/
10-Analytics/runtime/

# EventPulse Agent Runtime — local mission mirrors + runtime cache (regenerate on demand)
/.claude/eventpulse/missions/
/.claude/eventpulse/handoffs/
/.claude/eventpulse/state/
/.claude/eventpulse/evidence/
/.eventpulse-agent/

# Local scratch / debug dumps (excluded from tsconfig)
/tmp/
/claude-test-empty/

# 06-UI dead legacy copy (T0105, user decision 2026-09-04) — moved to /tmp backup.
# The live, actively developed originals are 02-Ingestion/ and root packages/shared.
# No code imports these; kept ignored so accidental restore attempts stay untracked.
/06-UI/services/ingestion/
/06-UI/packages/shared/
